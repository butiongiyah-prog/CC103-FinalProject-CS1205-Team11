//  void viewExpiryAlerts() {
//      ExpiryPriorityQueue pq;
//      for (int i = 0; i < itemCount; i++) pq.insert(items[i]);
//      while (!pq.isEmpty()) {
//          Product p = pq.extractMin(); // most urgent first
//          // display with color
//      }
//  }
//
//  case 6 in main():
//  {
//      sectionHeader("EXPIRY ALERTS");
//      cout << "Priority Queue (Min-Heap), heapify is recursive.\n";
//      inv.viewExpiryAlerts();
//      pressEnter();
//      break;
//  }
